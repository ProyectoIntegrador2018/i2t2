<%= form_with(model: company, local: true) do |form| %>
  <% if company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(company.errors.count, "error") %> prohibited this company from being saved:</h2>

      <ul>
      <% company.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="card mt-5">
    <div class="card-header">
      <h4>Datos Generales</h4>
    </div>
    <div class="card-body">
      <div class="form-group">
        <label for="inputNombre">Nombre de la empresa:</label>
        <%= form.text_field :name, class: "form-control", placeholder: "Ej. Farmville Corpo." %>
      </div>
      <div class="form-group">
        <label for="inputGiro">Giro de la empresa:</label>
        <%= form.text_field :industry, class: "form-control", placeholder:"Ej. Tecnología" %>
      </div>
      <div class="form-group">
        <label for="inputReni">No. de RENIECYT:</label>
        <%= form.text_field :reniecyt, class: "form-control", placeholder:"Ej. 123456ABC" %>
      </div>
      <div class="form-group">
        <label for="inputMunicipio">Municipio:</label>
        <%= form.text_field :location, class: "form-control", placeholder:"Ej. Guadalupe, N.L." %>
      </div>
    </div>
  </div>
  <div class="d-flex py-3">
    <div class="actions ml-auto">
      <%= form.submit "Confirmar ", class: "btn btn-outline-info" %>
    </div>
  </div>
<% end %>
